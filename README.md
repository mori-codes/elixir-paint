# Painting together

Silly test for a manual tcp server in elixir that allows multi client updates to a square mesh. Mostly to learn about tcp handling.

```elixir
# How to start the server

server = Server.accept(3000)
```


```js
// Serve the client html files
deno run --allow-read --allow-net jsr:@std/http/file-server
```







